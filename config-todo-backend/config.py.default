# ----------------------------------------------------------------------------------------------------------------------
# общее
# ----------------------------------------------------------------------------------------------------------------------

# хосты
LOCAL_HOST = 'localhost'
LOCAL_HOST_HTTP = 'http://localhost'

# ip-адреса
LOCAL_IP = '127.0.0.1'
LOCAL_IP_HTTP = 'http://127.0.0.1'

# ----------------------------------------------------------------------------------------------------------------------
# django project
# ----------------------------------------------------------------------------------------------------------------------

# ключи и пароли проекта django
SETTINGS_SECRET_KEY = ''

# конфигурация проекта django
DEBUG = True

APPEND_SLASH = False

ALLOWED_HOSTS = [

]

# минимальная длина пароля пользователя
PASSWORD_MIN_LENGTH = 8

# ----------------------------------------------------------------------------------------------------------------------
# rest framework
# ----------------------------------------------------------------------------------------------------------------------

REST_FRAMEWORK = {

    'DEFAULT_AUTHENTICATION_CLASSES': [
        'rest_framework.authentication.TokenAuthentication',
    ],
}

# ----------------------------------------------------------------------------------------------------------------------
# database
# ----------------------------------------------------------------------------------------------------------------------

# подключение к БД
POSTGRES_HOST = 'localhost'
POSTGRES_PORT = 5432
POSTGRES_USER = ''
POSTGRES_PASSWORD = ''
DB_NAME = ''

# ----------------------------------------------------------------------------------------------------------------------
# cors
# ----------------------------------------------------------------------------------------------------------------------

CORS_URLS_REGEX = r'^/api/.*$'

CORS_ALLOWED_ORIGINS = [
    'http://localhost',
    'http://localhost:3000',
]

# разрешённые принимаемые headers
CORS_ALLOW_HEADERS = [
    'accept',
    'accept-encoding',
    'authorization',
    'content-type',
    'dnt',
    'origin',
    'user-agent',
    'x-csrftoken',
    'x-requested-with',
]

# разрешённые возвращаемые headers
CORS_EXPOSE_HEADERS = [
    'content-type',
    'x-csrfoken',
]

CORS_ALLOW_CREDENTIALS = True

# ----------------------------------------------------------------------------------------------------------------------
# kafka
# ----------------------------------------------------------------------------------------------------------------------

# брокеры kafka
KAFKA_BROKERS = [

]

# ----------------------------------------------------------------------------------------------------------------------
# todo-ui
# ----------------------------------------------------------------------------------------------------------------------

# хост сервиса todo-ui
TODO_UI_HOST = 'http://localhost:3000'

# URL для подтверждения аккаунта
CONFIRM_ACCOUNT_URL = '/profileConfirmation?token='
